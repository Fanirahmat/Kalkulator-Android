<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sistelkocak">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Sistel Kocak"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".InputAnalog"></activity>
        <activity android:name=".Loss" />
        <activity android:name=".Gain" />
        <activity android:name=".DBw" />
        <activity android:name=".DBm" />
        <activity android:name=".Daya" />
        <activity android:name=".KalkulatorMenu" />
        <activity android:name=".GrafikSignal" />
        <activity android:name=".SistelMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Kalkulator" />
    </application>

</manifest>